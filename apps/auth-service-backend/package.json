{
  "name": "@aivo/auth-service",
  "version": "0.1.0",
  "private": true,
  "description": "Authentication Microservice",
  "scripts": {
    "dev": "uvicorn main:app --reload --port 8001",
    "build": "echo 'Python service - no build step'",
    "test": "pytest",
    "lint": "ruff check ."
  }
}
